<h1>My tasks</h1>

<ul>
  <% @tasks.each do |task| %>
    <li>
      <%= "#{task.completed ? "✔️" : "⬜️"} - "%>
      <%= link_to "#{task.title}", "tasks/#{task.id}" %>
    </li>
  <% end %>
</ul>
<br>


<%= link_to "Add New Task", tasks_new_path %>
